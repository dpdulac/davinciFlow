1. [IMPLEMENTED] Un-Hardcoding "TMNT2" (Dynamic Show Configs)
   - Moved all show-specific logic into a dynamic JSON/dict configuration structure (SHOW_CONFIGS, including agx.json, default, and tmnt2).
   - Dynamically evaluates allow_agx and project-level DRX color nodes based on Project name.

2. [IMPLEMENTED] Multi-Track A/B Wiping (Version Wipe & Task Wipe)
   - Supported across both Sequence Mode and Playlist Mode via a dedicated collapsible "A/B WIPE" UI section.
   - Version Wipe: Compares current candidate against an earlier version of the same task, filtering by any of the 22 valid ShotGrid statuses (e.g. rtk, rev, chk).
   - Task Wipe: Compares latest version of two distinct tasks (Top V2 candidate vs Bottom V1 reference) with mutually exclusive toggles.
   - Places reference takes on Track 1 (V1) and target candidate takes on Track 2 (V2) with 100% frame-accurate alignment.
   - Generates synchronized red Fusion Title placeholders ("NO PREV VERSION\n[shot_code]" or "NO REF TASK [TASK_NAME]\n[shot_code]") on V1 for missing reference media.
   - Robust Format Detection: Reliably evaluates media pool properties independent of customized display clip names to ensure uniform color grading (.drx nodes, AgX, or LUTs) applies across all layers.

3. [IMPLEMENTED] Visual Task Identification & Timeline Markers
   - Automatically colors clips on the timeline strip by pipeline department (e.g., compo->Blue, lighting->Yellow, anim->Green).
   - Renames timeline item display labels to cleanly format department tags directly across visual track blocks: `[TASK] shot version`.
   - Optional toggleable timeline marker flags attached to frame 0 displaying full Shot, Task, Version, and Track Layer metadata in tooltips and Edit Index.

4. Advanced Fusion Burn-ins / Data Slates
   - Expand the Fusion placeholder logic to create data burn-ins on all clips.
   - Dynamically print version_name, focal_length, animator_name, or date in the corner of the screen for dailies reviews.

5. Two-Way Sync (Pushing Notes back to ShotGrid)
   - Add a "Sync Notes" button to the UI.
   - Gather notes written inside DaVinci Resolve markers on the timeline and push them back to ShotGrid as official Review Notes attached to the shots.
